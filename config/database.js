var config = {
    connectionLimit: 100,
    host: 'localhost',
    user: 'root',
    password: 'local',
    database: 'test',
    port: 3310,
    debug: false,
    multipleStatements: true
};
module.exports.config = config;